# Core
python-dotenv==1.0.1

# PDF ingestion
pymupdf==1.24.10

# Tokenization / chunking
tiktoken==0.7.0

# Embeddings
sentence-transformers==3.1.1
torch==2.4.1

# Retrieval
rank-bm25==0.2.2

# Vector store
chromadb==0.5.5
posthog==3.7.4  # pinned: chromadb 0.5.5's posthog calls break on posthog>=4
faiss-cpu==1.14.3
qdrant-client==1.18.0

# LLM generation
google-genai==2.9.0
anthropic==0.34.2  # kept for reference/fallback; primary backend is Gemini (see core/generation.py)
httpx==0.27.2  # pinned: anthropic 0.34.2 passes 'proxies' kwarg removed in httpx>=0.28

# UI
streamlit==1.38.0

# Evaluation / utils
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.5
requests==2.32.3

# ArXiv fallback for corpus
arxiv==2.1.3
